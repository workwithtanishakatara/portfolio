/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', "api.microlink.io", "cdn.sanity.io", "scontent.cdninstagram.com"], // Add your desired domain(s)
       
      },
};

export default nextConfig;
